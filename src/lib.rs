pub mod time;
pub mod bit;
pub mod slice;

/// # Fantasy Util
/// ## Jelipo custom util lib.
/// Include *time*,*bit* tool.
///

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
